# Background

`bh_demo` models the tutorial Buchwald-Hartwig reaction dataset from `examples/BH/BH_dataset.csv`.
The benchmark uses feature selection plus a learned oracle to expose a compact continuous search space.

This is a smoke-level benchmark task.
It is not a full reaction-optimization reproduction.
