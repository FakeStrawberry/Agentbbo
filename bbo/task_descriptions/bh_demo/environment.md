# Environment

Recommended setup:

```bash
uv sync --extra dev --extra bo-tutorial
```

The task requires `pandas` and `scikit-learn`.
Tutorial data are staged from `BBO_BO_TUTORIAL_SOURCE_ROOT` into the benchmark cache before evaluation.
