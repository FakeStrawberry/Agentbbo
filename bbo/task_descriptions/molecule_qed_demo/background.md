# Background

`molecule_qed_demo` exposes the tutorial molecule task based on the archive `examples/Molecule/zinc.txt.gz`.
The search space is a categorical list of SMILES strings staged from the tutorial repository.

This benchmark keeps the real RDKit QED objective.
It does not use a surrogate or fake molecular score.
