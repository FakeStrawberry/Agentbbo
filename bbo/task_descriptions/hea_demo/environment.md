# Environment

Recommended setup:

```bash
uv sync --extra dev --extra bo-tutorial
```

The task requires `pandas`, `openpyxl`, and `scikit-learn`.
Set `BBO_BO_TUTORIAL_SOURCE_ROOT` if the tutorial checkout is not in the default cache-discovery locations.
