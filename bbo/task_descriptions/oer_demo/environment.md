# Environment

Recommended setup:

```bash
uv sync --extra dev --extra bo-tutorial
```

The task requires `pandas` and `scikit-learn`.
The tutorial checkout can be supplied through `BBO_BO_TUTORIAL_SOURCE_ROOT`, and staged assets are cached under the benchmark artifact cache.
